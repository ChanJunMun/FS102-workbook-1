function minNum (firstNum, secNum) {
    if (firstNum < secNum) {
        console.log ("The smaller number is " + firstNum);
    } else {
        console.log ("The smaller number is " + secNum);
    }
}

minNum (1, 1)